# ── SECRETS — never commit ────────────────────────────────────
local.properties
*.keystore
*.jks
keystore.properties
secrets.properties
.env*
!.env.example

# ── Android build outputs ─────────────────────────────────────
/build
/app/build
*.apk
*.aab

# ── Gradle ────────────────────────────────────────────────────
.gradle/
gradle-wrapper.jar
!gradle/wrapper/gradle-wrapper.properties

# ── Android Studio / IntelliJ ─────────────────────────────────
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/

# ── Node (keeping your original) ──────────────────────────────
node_modules/
dist/
coverage/
*.log


# Local backups of original Joe clips before studio-audio replacement
app/assets/audio/_backup_joe/

# Ruby / fastlane bundled gems (installed locally via `bundle config set --local path 'vendor/bundle'`)
app/android/vendor/
vendor/bundle/
.bundle/

# Manifest backups
app/assets/audio/manifest.json.bak
*.bak
